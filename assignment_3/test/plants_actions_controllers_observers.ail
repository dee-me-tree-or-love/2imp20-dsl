Module : { TEST_ACTIONS_CONTROLLERS_OBSERVER_AND_PLANTS }

Plants : {
    Garlic <<
        water_use : 5,
        min_temp  : 20,
        max_water : 100,
    >>,
    Potato <<
        water_use : 5,
        min_temp  : 20,
        max_water : 100,
        breed : "OpperdoezerRonde",
    >>
}

Actions : {
    notify <<%src, foo>> (
        bar + foo;
        foo + bar;
        quu = foo + (bar + oof);
        loo = foo.attr;
        too = bar.attr.other;
        if foo meets * > 30 then do (alpha) else do (beta);
        if foo meets * > bar.qoo then do alpha else do (beta);
        if foo meets * > bar.qoo 
        then do
            alpha; beta; gamma; 
        else do
            nu; mu; psi;
        ;
        if foo meets some_condition then do (alpha) else do (beta);
        notify : {bar};
        bars @> {b | b + 1 };
        bars #> {b | b ~~ 24} @> {b | b + 1 };
    )
}

Observers : {
    support << "support@farm.cool" >>, 
    admin << "admin@farm.cool" >>, 
    rss << "farm.feed.rss" >>,
    abie << "abie@service.farm.cool" >>
}
