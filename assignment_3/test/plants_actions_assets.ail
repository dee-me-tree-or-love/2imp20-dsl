Module : { TEST_GARLIC }

Plants : {
    Garlic <<
        approx_volume : 1 \cm^2,
        water_use : 5 \mm,
        min_temp  : 20 \C,
        max_water : 100,
    >>
}

Actions : {
    notify <<%src, channels>> (
        channels @> {c | send_message : {c, "hello", %src}};
    )
}

